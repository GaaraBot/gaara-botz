const welcome = (number, groupname) => {
    return `Iaee @${number}. Seja Bem vindo meu mano ${groupname}`
}
exports.welcome = welcome

const bye = (number) => {
    return `Tchauu @${number}. isso me deixa triste 👋`
}
exports.bye = bye